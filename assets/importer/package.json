{
  "name": "medipro-importer",
  "version": "1.0.0",
  "private": true,
  "description": "ETL pipeline: legacy medipro.db + product_list.xlsx → seed.db + catalog.db",
  "scripts": {
    "import": "node run-import.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
